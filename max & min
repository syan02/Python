# Find Min & Max element in an array of int. using Single Loop

a = [2, 5, 8, 2, 7, 2, 1]
max = a[0]
min = a[0]
i=1
while i < len(a) :
    if max < a[i]:
        max = a[i]
    if min > a[i]:
        min = a[i]
    i+=1
print("Max = ", max)
print("Min = ", min)
